import type { Options } from "prettier";

const config: Options = {
  endOfLine: "auto",
  semi: true,
  singleQuote: false,
  trailingComma: "es5",
  printWidth: 100,
  tabWidth: 2,
  bracketSpacing: true,
  arrowParens: "always",
};

export default config;
